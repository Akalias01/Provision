<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Cover art stored in app's internal files directory -->
    <files-path name="covers" path="." />

    <!-- Cache directory for temporary files -->
    <cache-path name="cache" path="." />

    <!-- External files directory if covers are stored there -->
    <external-files-path name="external_covers" path="." />
</paths>
